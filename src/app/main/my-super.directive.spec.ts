import { MySuperDirective } from './my-super.directive';

describe('MySuperDirective', () => {
  it('should create an instance', () => {
    const directive = new MySuperDirective();
    expect(directive).toBeTruthy();
  });
});
